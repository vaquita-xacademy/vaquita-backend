export enum UserRole {
    ADMIN = "admin",
    DONOR = "donor",
    OWNER = "owner",
}

export enum VerifiedProfileStatus {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
}

export enum EntityType {
    INDIVIDUAL = "individual",
    LEGAL = "legal"
}

